ALGORITHM InsertIonSort
VAR
    i= INTEGER;
    j= INTEGER;

BEGIN
  For each element i in the array, starting from the second element (i = 1):
    Set a key = i-th element of the array.
    Set a j =i - 1.
    While j >= 0 and the j-th > key:
    Shift the j-th element of the array to the right by one position.
    ii. Decrement j by one.
    Insert the key variable into the (j + 1)-th position in the array.
Return the sorted array.

END


